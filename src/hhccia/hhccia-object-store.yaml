apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: hhccia-object-store
  namespace: hhccia
spec:
  configuration:
    destinationPath: s3://backups-k8s/hhccia-cluster
    endpointURL: https://77df3d66af9eb572fe180d800d44127b.r2.cloudflarestorage.com
    s3Credentials:
      accessKeyId:
        name: backup-creds-secret
        key: accessKeyId
      secretAccessKey:
        name: backup-creds-secret
        key: secretAccessKey
    wal:
      compression: gzip
    data:
      compression: gzip
