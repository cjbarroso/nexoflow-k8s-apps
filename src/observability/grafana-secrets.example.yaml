# PLACEHOLDER — DO NOT APPLY, DO NOT COMMIT REAL VALUES.
# Excluded from the `observability-secrets` Argo app. Copy these keys, fill real
# values, seal with kubeseal, and commit the output as
# grafana-secrets-sealedsecret.yaml (see README.md).
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secrets
  namespace: observability
type: Opaque
stringData:
  admin-user: admin
  admin-password: CHANGE_ME
  # From the Authentik OAuth2/OpenID provider for the `grafana` application.
  oidc-client-id: CHANGE_ME
  oidc-client-secret: CHANGE_ME
