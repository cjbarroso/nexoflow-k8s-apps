apiVersion: v1
kind: Service
metadata:
  name: sftp
  namespace: sftp
  labels:
    app: sftp
spec:
  type: ClusterIP
  selector:
    app: sftp
  ports:
    - name: ssh
      protocol: TCP
      port: 22
      targetPort: ssh
