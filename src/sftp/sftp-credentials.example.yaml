apiVersion: v1
kind: Secret
metadata:
  name: sftp-credentials
  namespace: sftp
type: Opaque
stringData:
  # Generate a random token and keep the plaintext outside the repository.
  UPLOAD_TOKEN: "REPLACE_WITH_LONG_RANDOM_TOKEN"
