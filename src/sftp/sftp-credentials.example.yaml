apiVersion: v1
kind: Secret
metadata:
  name: sftp-credentials
  namespace: sftp
type: Opaque
stringData:
  # atmoz/sftp syntax: user:password:uid:gid:directory
  # The password must not contain ':' or whitespace.
  SFTP_USERS: "sftp:REPLACE_WITH_LONG_RANDOM_PASSWORD:1000:1000:upload"
